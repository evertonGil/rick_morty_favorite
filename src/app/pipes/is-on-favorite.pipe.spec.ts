import { IsOnFavoritePipe } from './is-on-favorite.pipe';

describe('IsOnFavoritePipe', () => {
  it('create an instance', () => {
    const pipe = new IsOnFavoritePipe();
    expect(pipe).toBeTruthy();
  });
});
